# Configuration SMTP pour Envoi Automatique d'Emails de Réservation
# Copier ces variables d'environnement dans votre fichier .env

# ============================================
# CONFIGURATION SMTP - Choisir un fournisseur
# ============================================

# OPTION 1: Gmail (Simple, gratuit)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=votre-email@gmail.com
SMTP_PASS=mot-de-passe-application-google
SMTP_SECURE=false

# OPTION 2: SendGrid (Production-ready)
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=SG.xxxxxxxxxxxxxxxxxxxxx
# SMTP_SECURE=false

# OPTION 3: Mailgun (Alternative)
# SMTP_HOST=smtp.mailgun.org
# SMTP_PORT=587
# SMTP_USER=postmaster@votre-domaine.mailgun.org
# SMTP_PASS=votre-mot-de-passe
# SMTP_SECURE=false

# OPTION 4: OVH (Pour domaines OVH)
# SMTP_HOST=ssl0.ovh.net
# SMTP_PORT=465
# SMTP_USER=votre-email@votre-domaine.com
# SMTP_PASS=votre-mot-de-passe
# SMTP_SECURE=true

# ============================================
# EMAIL DE CONTACT & ENTREPRISE
# ============================================
ADMIN_EMAIL=admin@votresite.com
CONTACT_EMAIL=contact@votresite.com
CONTACT_PHONE=+33 00 00 000
COMPANY_NAME=Votre Nom d'Entreprise

# ============================================
# MODE D'EMPLOI
# ============================================
# 1. Choisir un fournisseur SMTP ci-dessus
# 2. Remplacer les variables par vos identifiants
# 3. Ajouter à votre fichier .env du backend
# 4. Redémarrer le serveur backend (npm run dev)
# 5. Les emails s'enverront automatiquement !
